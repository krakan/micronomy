services:
  micronomy:
    build: .
    ports:
      - "8080:8080"
    environment:
      MICRONOMY_HOST: "0.0.0.0"
      MICRONOMY_PORT: "8080"
    volumes:
      - demo-data:/app/resources
    restart: unless-stopped
 
volumes:
  demo-data:
